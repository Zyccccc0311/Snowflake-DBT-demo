name: dbt_test_project
version: 1.0.0
config-version: 2
profile: dbt_test_project
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots
models:
  dbt_test_project:
    +materialized: view
